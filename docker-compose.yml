name: niche-analysis

services:
  dashboard:
    image: nginx:1.27-alpine
    container_name: niche-analysis-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:8765:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
